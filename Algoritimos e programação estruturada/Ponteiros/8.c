#include <stdio.h>
#include <math.h>
#include <stdlib.h>

main()
{

    float valor[10];

    for (int i = 0; i < 10; i++)
    {

        i + 1;

        printf("\nEndereco do elemento na posicao %d: %d", i, &valor[i]);
    }
}